services:
  - type: web
    runtime: python
    name: ASL-CNN
    buildCommand: |
      apt-get update && apt-get install -y git-lfs
      git lfs install
      git lfs pull
      pip install -r requirements.txt
    startCommand: gunicorn app:app
